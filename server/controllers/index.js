const user = require('./c_users');
const drugs = require('./c_drugs');
const rights = require('./c_rights');
const orders = require('./c_orders');
const dogstats = require('./c_hot_shots');

// const todoItems = require('./todoitems');

module.exports = {
    user,
    drugs,
    rights,
    orders,
    dogstats
};